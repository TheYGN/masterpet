# Excalidraw Sync — Punch List

> **קובץ חי.** מתעד שינויים שצריך לסנכרן מ-`prd/feature-tree.md` ל-`pet_platform_tree.excalidraw` (העץ הוויזואלי). הסנכרון נעשה **ידנית** ע"י ירין דרך Excalidraw UI.
>
> **חוק זהב מ-CLAUDE.md:** "סנכרון עץ אפיון — חובה דו-כיוונית." השניים חייבים תמיד לתאר את אותו מצב. אם זוהה drift — להריץ Diff Check ולתקן לפני שממשיכים בעבודה אחרת.

**עדכון אחרון:** 2026-05-30

---

## פתוח לסנכרון — צריך עדכון ב-Excalidraw

(ריק — אין סנכרונים פתוחים)

---

## היסטוריה — סנכרונים שבוצעו

### 2026-05-30 — PRD #19a (Invoicing & Documents) — ✅ בוצע אוטומטית ע"י טרוי

**שיטה:** סקריפט Node.js (`/.scratch/sync-excalidraw.js`) ערך את ה-JSON ישירות. הגיבוי נשמר ב-`pet_platform_tree.excalidraw.backup-2026-05-30`.

**7 שינויים שבוצעו:**

| # | אלמנט | מה השתנה |
|---|---|---|
| 1 | `e0307` (מקום ראשי באינטגרציות) | `[MVP] Morning (Greeninvoice)` → `[MVP] PayPlus Invoices — חשבוניות (InvoiceProvider, PRD #19a)` |
| 2 | `e0928` (חדש) | נוסף `[P2] Morning (Greeninvoice) — דחוי ל-P2 (PRD #19a)` מתחת לעמודת האינטגרציות |
| 3 | `e0459` (כותרת הנה"ח) | `הנהלת חשבונות פנימית` → `הנהלת חשבונות פנימית (19a MVP + 19b P2)` |
| 4 | `e0929` + `e0930` (חדשים) | נוסף בלוק 19a מתחת לעמודת הנה"ח עם כל 10 פריטי ה-MVP + ציון מה ב-P2 |
| 5 | `e0342` (Tech stack) | `PayPlus / Morning (Greeninvoice)` → `PayPlus (סליקה + חשבוניות — MVP) / Morning (Greeninvoice) — P2` |
| 6 | `e0433` (שוטף 30/60) | נוסף `(תלוי ב-PRD #19a)` |
| 7 | `e0320` (Sprint 7-8 plan) | נוספה שורה `• Invoicing & Documents (PRD #19a — PayPlus, ~24.5 ימים)` + עדכון אשראי לקוחות לציון חסימה |

**אומת:** 521 אלמנטים (היה 518), 0 duplicate IDs, JSON valid, schema preserved (version 2, source, appState, files).

---

### 2026-05-30 (תיקון) — שכבת הסטיילינג הוויזואלי של 19a

**בעיה שזוהתה (ירין):** השינויים מ-Change 4 נוספו כ-**טקסט חופשי בלי מלבנים**, נראים תלושים מהעץ. כל פריט בעץ הוא **זוג** = מלבן ירוק (`#dcfce7` bg, `#16a34a` stroke) + טקסט בתוכו (containerId-bound).

**תיקון שבוצע (`.scratch/fix-excalidraw-boxes.js`):**
1. נמחקו 3 האלמנטים הצפים (e0928, e0929, e0930)
2. נוצרו **31 אלמנטים חדשים** עם המלבנים — כולם מקושרים `boundElements`/`containerId`:
   - כותרת סקציה (navy `#0f172a`) "19a. מסמכים חשבונאיים" + badge "MVP — Sprint 7-8" — כמו כל סקציה אחרת בעץ
   - 10 קופסאות MVP בירוק (זוגות rect+text) — כל 19a פריט
   - 3 קופסאות P2 בענבר (`#fef3c7` bg, `#d97706` stroke) — תת-סקציה למה שדחוי ל-Phase 2
   - 1 קופסת P2 בענבר ל-Morning בעמודת האינטגרציות

**הקופסאות החדשות:**
- 19a Section בעמודה x=5427.5, y=1230 ומטה (sub-section מתחת לפריטי הנה"ח הקיימים)
- Morning P2 בעמודה x=4247.5, y=1155

**אומת:** 549 אלמנטים (היה 521), 0 duplicate IDs, כל ה-`rect→text` bindings תקינים, schema preserved.

**גיבויים:**
- `pet_platform_tree.excalidraw.backup-2026-05-30` (לפני כל השינויים)
- `pet_platform_tree.excalidraw.backup-2026-05-30-v2` (לפני התיקון של הסטיילינג)

---

### 2026-05-30 (תיקון 2 — סוף סוף משולב) — חצים + צבעים נכונים

**בעיה שזוהתה (ירין, סבב שני):** הסקציה החדשה הופיעה **תלושה מהעץ**, רחוקה מהשרשרת. סיבות:
1. **אין חצים מחברים** בין הסקציה החדשה לפריטים הקיימים בעמודה
2. **צבע P2 שגוי** — השתמשתי בענבר במקום בכחול שזה הקונבנציה האמיתית

**תיקון סופי (`.scratch/sync-excalidraw-v3.js` + patch צבעים):**
1. שוחזר מהגיבוי המקורי
2. נבנה מחדש כך שכל הפריטים בעמודת הנה"ח **ממשיכים את השרשרת** — מ-y=1080 ממש אחרי הפריט הקיים האחרון ב-y=1005
3. נוסף **חץ אנכי** (slate gray `#94a3b8`, 20px) **בין כל זוג פריטים סמוכים** — 14 חצים חדשים
4. צבע P2 תוקן מענבר לכחול (`#dbeafe` bg, `#2563eb` stroke, `#1e3a8a` text) — 9 אלמנטים תוקנו

**תוצאה:** שרשרת רציפה מהכותרת ב-y=300 עד הפריט האחרון ב-y=1980, ללא תלישות. סה"כ 560 אלמנטים.

---

## חוקיות עיצובית של העץ — לסנכרונים עתידיים (גרסה מתוקנת)

> **3 חוקי זהב:** (1) כל פריט = זוג מלבן+טקסט. (2) **כל זוג סמוך = חץ ביניהם**. (3) צבע = סטטוס, לא סקציה.

### צבעים לפי סטטוס

| סטטוס | רקע | מסגרת | טקסט |
|---|---|---|---|
| **כותרת סקציה** | `#0f172a` (navy) | `#0f172a` | `#f8fafc` (לבן) |
| **MVP** | `#dcfce7` (ירוק בהיר) | `#16a34a` (ירוק) | `#14532d` (ירוק כהה) |
| **P2** | `#dbeafe` (כחול בהיר) | `#2563eb` (כחול) | `#1e3a8a` (כחול כהה) |
| **חץ מחבר** | — | `#94a3b8` (slate) strokeWidth=2 | — |

### גדלים ופונט

| סוג | רוחב×גובה | פונט |
|---|---|---|
| כותרת סקציה | 250×80 | 13 |
| פריט (MVP/P2) | 245×55 | 11 |
| חץ מחבר | 0×20 (אנכי) | — |
| Badge מעל סקציה | אוטומטי | 12 |

### ריווח

- **בין שורות בעמודה:** 75px (y delta)
- **גובה החץ:** 20px (יושב בין y+55 של פריט עליון ל-y של פריט תחתון)
- **כותרת סקציה ↔ פריט ראשון:** 180px gap
- **בין עמודות:** 295px (x delta)
- **roundness:** type 3 (מעוגל), `fillStyle: 'solid'`

### מבנה זוג (חובה)

```javascript
rect.boundElements = [{ type: 'text', id: textId }]
text.containerId = rectId
```

### מבנה חץ מחבר (חובה לכל זוג סמוך)

```javascript
{
  type: 'arrow',
  x: columnCenterX,        // = itemX + itemWidth/2
  y: previousItemY + 55,   // bottom of previous item
  width: 0, height: 20,
  strokeColor: '#94a3b8',
  strokeWidth: 2,
  points: [[0, 0], [0, 20]],
  endArrowhead: 'arrow',
  startArrowhead: null,
  roundness: { type: 2 },
  startBinding: null, endBinding: null  // arrows are NOT bound
}
```

### Checklist לפני שמירה

- [ ] כל פריט חדש הוא **זוג** (rect + text)
- [ ] text.containerId == rect.id
- [ ] rect.boundElements כולל את textId
- [ ] **חץ מחבר** בין כל זוג פריטים סמוכים בעמודה
- [ ] צבע לפי סטטוס: MVP=ירוק, P2=כחול (לא ענבר, לא צהוב)
- [ ] IDs חדשים מתחילים מ-`max existing + 1` (אסור להתנגש עם IDs קיימים — אצלנו יש arrows ב-e0900-0927)
- [ ] אם הפריטים בעמודה קיימת — מיקום ב-y שמשך את השרשרת (75px מהאחרון)

---

## תיעוד הלמידה משלושת הנסיונות

| נסיון | בעיה | למה זה קרה | מה למדנו |
|---|---|---|---|
| 1 | טקסט חופשי בלי מלבנים | לא בדקתי שכל פריט הוא זוג rect+text | פריט = זוג |
| 2 | קופסאות מסוגננות אבל תלושות | לא בדקתי איך הפריטים מחוברים זה לזה | חץ בין כל זוג סמוך |
| 3 | צבע P2 שגוי (ענבר במקום כחול) | הנחתי שאני יודע איך נראה P2 | לבדוק צבע מפריט P2 קיים |

**מסקנה תפעולית:** לפני סנכרון Excalidraw — **לחקור את הקונבנציה במלואה** (סטיילים + חיבורים + צבעים + מיקום בשרשרת), לא רק את החלק שהמשתמש מצביע עליו. אחרת מתקנים אותו דבר 3 פעמים.

---

## מבנה הסנכרון

כל entry חדש כולל:
1. תאריך + PRD מקור
2. מה השתנה ב-`feature-tree.md` (טקסטואלי)
3. מה צריך לעדכן ב-Excalidraw (איזה כיתוב, באיזה סעיף, איזה צבע אם רלוונטי)

כשירין מבצע את הסנכרון:
1. פותח את `pet_platform_tree.excalidraw`
2. מעדכן לפי ההוראות
3. מעביר את ה-entry מ-"פתוח" ל-"היסטוריה"
